#include <bits/stdc++.h>

#define FF first
#define SS second
#define MP make_pair

using namespace std;

using ll = long long int;
using ii = pair<int, int>;

constexpr int MOD = 1e9+7;

void solve(){
    int n, m, q; cin >> n >> m >> q;
    string s; cin >> s;
    vector<ii> r(m);
    for(int i = 0; i < m; ++i) cin >> r[i].FF >> r[i].SS;

    while(q--){
        int x; cin >> x;
        
    }
}

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0); cout.tie(0);

    int _t; cin >> _t;
    while(_t--)
        solve();

    return 0;
}